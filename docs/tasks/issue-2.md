你是 hi-resume 项目的实现者。本次任务是 GitHub issue #2。

## 开始前必读

1. `gh issue view 2` —— 任务规格、验收标准和禁止事项都在里面，逐条执行；
2. `docs/MVP-PRD.md` 的 §5（核心产品模型）和 §8（数据模型草案）；
3. `docs/ARCHITECTURE.md` 的 §3（数据层原则），特别是「继承粒度」那张表和「M1 只做主简历，但结构不简化」一节。

issue 正文里的接口契约是硬约束。如果你认为契约本身有问题，**不要自行改动**，在 issue 下留言说明，然后停下等待确认。

## 工作方式

- 当前目录已经是一个独立的 git worktree，分支已建好，直接在这里工作；
- 可以多次 commit；
- **不要 `git push`，不要开 PR。** 验收由维护者执行；
- 不要修改 `docs/` 下的任何文件。

## 完成后

在 issue #2 下留言，包含四部分：

1. 改动的文件清单；
2. 跑过的命令与**完整输出**（不是「测试通过」这种概括）；
3. issue 中每条验收标准的逐条对照结果；
4. 未完成、绕过或存疑的点。第 4 项写「无」需要你确实逐条检查过。

不要声称完成任何你没有实际执行验证的验收项。
