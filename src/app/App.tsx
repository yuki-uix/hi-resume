// Placeholder shell. The editor UI arrives in later M1 tasks; this task only
// ships the domain layer, and the app entry exists so the scaffold is runnable.
export function App() {
  return <main>hi-resume</main>
}
